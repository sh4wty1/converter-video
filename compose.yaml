services:
  postgres:
    image: 'postgres:17'
    environment:
      - 'POSTGRES_DB=videojobs'
      - 'POSTGRES_PASSWORD=app'
      - 'POSTGRES_USER=app'
    ports:
      - '5432:5432'
    volumes:
      - 'pgdata:/var/lib/postgresql/data'

volumes:
    pgdata: